# MacBook Duo 完整源码

本仓库包含菜单栏应用、实时桌面与截图测试、锁屏墙纸扩展、息屏唤醒动画、图标和开发工具。编译产物、验证数据及本机历史文档通过 `.gitignore` 排除。

## 构建

在 Mac 上安装 Apple Command Line Tools（终端运行 `xcode-select --install`），进入本目录后运行：

```sh
./build.sh
open "MacBook Duo.app"
```

脚本调用系统 Swift 编译器，生成当前 Mac 架构的应用，并进行 ad-hoc 本机签名；不需要下载第三方依赖。Metal 着色器源码内嵌于 Swift 文件中，在运行时编译。项目没有 Xcode 工程文件。

当前 `Info.plist` 将最低系统版本设为 macOS 26.0。构建脚本按当前机器架构编译，重新构建的结果仍会受本机 Swift/Metal 工具链影响。

## 源码结构

- Sources/HingeGlassApp.swift：应用入口、窗口、菜单和快捷键。
- Sources/RootView.swift：首页及调试界面。
- Sources/AppModel.swift：截图、参数、校准及应用状态。
- Sources/GlassRenderer.swift：Metal 玻璃与透视渲染。
- Sources/LidAngleSensor.swift：铰链传感器读取。
- Sources/GlobalDesktopController.swift：实时桌面捕获、全局覆盖层及休眠恢复逻辑。
- Sources/WallpaperBridgeWriter.swift：把主 App 的同一份开合快照发布给锁屏墙纸扩展。
- WallpaperPrototype/Extension/：WallpaperExtensionKit 适配、设置项和锁屏远程渲染实现；由根目录 `build.sh` 嵌入主 App。
- WallpaperPrototype/Shared/WallpaperPaths.swift：主 App 与沙箱扩展共享的自定义壁纸和角度桥路径。
- Sources/ScreenCapturePermissionPreparation.swift：屏幕录制权限准备。
- Sources/DesktopWakeAnimation.swift：未锁屏息屏后的唤醒动画状态、计时及耗时选项。
- Tests/：角度、运行策略、权限、记录、桌面唤醒和渲染生命周期测试。
- Assets/：应用图标及设计记录。
- Info.plist、build.sh：应用元数据与构建入口。

## 注意

实时桌面模式需要屏幕录制权限及兼容的铰链传感器。当前测试版包含按构建标识进行首次权限重置的逻辑，重新编译后可能需要再次授权。不要同时运行多个同标识的副本。自有代码采用 PolyForm Noncommercial 1.0.0 非商用许可；第三方代码保留原许可，详见 `LICENSE` 与 `THIRD_PARTY_NOTICES.md`。

## 开发工具

`Tools/validate-ui.sh` 检查设置页面与菜单，`Tools/test-renderer.sh` 验证渲染生命周期；输出位于不提交的 `Validation/`。

`Tools/build-benchmarks.sh` 是历史对比工具，需要自行提供 `Validation/Baseline/Sources/` 下的旧版 `GlassRenderer.swift`、`HingeMotion.swift` 和 `LidAngleSensor.swift`。这些基线未随源码发布；普通构建和测试不依赖它们。其他性能与报告工具按各自参数读取本地采集数据，不包含历史测量结果。
